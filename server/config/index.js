const config = {
    port: 8080,
    database: {
        DATABASE: 'e_tickets',
        USERNAME: 'tickets_admin',
        PASSWORD: 'tickets',
        HOST: 'localhost',
        PORT: 3306
    }
};

module.exports = config;